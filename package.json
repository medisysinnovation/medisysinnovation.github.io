{
  "name": "root",
  "private": true,
  "author": "Medisys <medisysadmin@medinno.com>",
  "devDependencies": {
    "lerna": "^3.22.1",
    "microbundle": "^0.13.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "build:compress": "lerna run build:compress",
    "test": "lerna run test"
  }
}
